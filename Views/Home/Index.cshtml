@using Microsoft.AspNetCore.Identity
@model IEnumerable<userMangment.Models.ApplicationUser>
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Home Page";
}




@if (TempData["Error"] != null)
{

    <div class="alert alert-danger col-6  alert-dismissible fade show" role="alert">
        <p> @TempData["Error"]</p>

        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success col-6  alert-dismissible fade show" role="alert">
        <p> @TempData["Success"]</p>

        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Time of registration</th>

        </tr>
    </thead>
    <tbody>

       
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.FullName</td>

                <td>@user.TimeOfRegistration</td>

                <td>
                    @if (!await UserManager.IsInRoleAsync(user, "Member"))
                    {

                        <form action="MakeMember" method="post">
                            <input type="hidden" name="userId" value="@user.Email" />

                            <button type="submit" class="btn btn-sm btn-success">Set as a subscriber</button>

                        </form>
                    }
                    else
                    {
                        <span class="badge bg-primary">actual subscriber</span>
                    }

                </td>
            </tr>
        }

       
        <tr>
            <td>
                <form asp-controller="Home" asp-action="ChangePassword" method="Post">

                    <div class="form-group row">
                        <label for="currentPassword" class="col-sm-2 col-form-label">current password</label>
                        <div class="col-sm-10">
                            <input type="password" name="currentPassword" class="form-control" id="inputPassword3"
                                   placeholder="Password">
                            <span class="text-danger"
                            @ViewData.ModelState[""]?.Errors.FirstOrDefault()?.ErrorMessage></span>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label for="newPassword" class="col-sm-2 col-form-label">new password</label>
                        <div class="col-sm-10">
                            <input type="password" name="newPassword" class="form-control" id="inputPassword3"
                                   placeholder="Password">
                        </div>
                    </div>

                            
                    <div class="form-group row">
                        <label for="confirmPassword" class="col-sm-2 col-form-label">confirmPassword password</label>
                        <div class="col-sm-10">
                            <input type="password" name="confirmPassword" class="form-control" id="inputPassword3"
                                   placeholder="Password">
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary mt-2">Change Password</button>
                </form>
            </td>
        </tr>

             
            
       
    </tbody>

</table>

@* <div class="text-center">
    <h1 class="display-4">Welcome</h1>

    @if (User.Identity.IsAuthenticated)
    {
        <p>Hello : @User.Identity.Name</p>
    }
    else

{
        <p>You are visitor</p>

} *@




  
@* <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>


    


   

    
 @if (Model != null)
{

    <table class="table">
        <thead>
            <tbody>
                <tr>
                    <th scope="row">1</th>
                    <th scope="row">2</th>

                </tr>
            </tbody>

</thead>
        <tbody>

@foreach (var item in Model)
            {
                if (item.AttendingMembers != null)
                {
                    foreach (var member in item.AttendingMembers)
                    {

<tr>
                            <td>@string.Join(", ", item.Name)</td>

                               
                            <td> <span class="badge bg-info text-dark me-1">@string.Join(" ",
item.AttendingMembers.Select(m=>m.UserName))</span></td>

                               
                            </tr>

                        }
                    }

                }

             
            </tbody>

        </table>
    }
    else

{
        <p>لا توجد حصص مضافة حالياً في قاعدة البيانات.</p>

}


      
@* <form asp-action="CreatClass">
        <div asp-validation-summary="All"></div>
        <label>Name</label>
        <input type="text" asp-for="Name">
            <span asp-validation-for="Name"></span>

            <label>Start Time</label>
            <input type="datetime-local" asp-for="StartTime" />
            <span asp-validation-for="StartTime"></span>

            <label>Duration</label>
            <input type="time" asp-for="Duration" />
            <span asp-validation-for="Duration"></span>

            <label>Desctiption</label>
            <input type="text" asp-for="Desctiption" />
            <span asp-validation-for="Desctiption"></span>

            <button type="submit">Creat Class</button>

        </form> *@
@* </div> *@
